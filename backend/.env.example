# Server
PORT=4000

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=expense_tracker

# Gemini API (LLM parsing / category matching)
GEMINI_API_KEY=
# Optional: override the model (default: gemini-3.5-flash)
GEMINI_MODEL=gemini-3.5-flash
